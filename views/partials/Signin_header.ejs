<!DOCTYPE html>
<html>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <script src="/Signin.js" defer></script>
    <link rel="stylesheet" type="text/css" href="/SignIn.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel = "icon" href = "/photo/logoGern.png" type = "image/x-icon">
    <head>
        <title>FLEX</title>
        
    </head>
    <% if(logout && logout.length > 0) { %>
        <div id="success-bar"><span class="check fa fa-check"></span><%= logout %></div>
    <% } %>
    <% if(remove && remove.length > 0) { %>
        <div id="success-bar"><span class="check fa fa-check"></span><%= remove %></div>
    <% } %>
    <% if(notlogin && notlogin.length > 0) { %>
        <div id="fail-bar"><span class="fail fa fa-times"></span><%= notlogin %></div>
    <% } %>
    <% if(changepassword && changepassword.length > 0) { %>
        <div id="success-bar"><span class="check fa fa-check"></span><%= changepassword %></div>
    <% } %>
    <% if(userverify && userverify.length > 0) { %>
        <div id="verify-bar"><span class="verify fa fa-exclamation"></span><%= userverify %></div>
    <% } %>
